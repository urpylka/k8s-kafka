apiVersion: v1
kind: Pod
metadata:
  name: debian
  namespace: default
spec:
  containers:
  - image: debian
    command:
      - sleep
      - "36000"
    imagePullPolicy: IfNotPresent
    name: debian
  restartPolicy: Always